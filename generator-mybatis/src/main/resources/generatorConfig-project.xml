<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <properties resource="config/jdbc-project.properties"/>

    <classPathEntry location="./src/driver/lib/mysql-database-driver.jar"/>

    <!-- 一个数据库一个context -->
    <!-- defaultModelType:flat 大数据字段,不分表 -->
    <context id="CossTables" targetRuntime="MyBatis3Simple" defaultModelType="flat"><!--MyBatis3-->
        <property name="useActualColumnNames" value="${extend.table.useActualColumnNames}"/>
        <property name="autoDelimitKeywords" value="true"/>
        <!-- 生成的pojo，将implements Serializable -->
        <plugin type="org.mybatis.generator.plugins.SerializablePlugin"/>
        <!-- 旨在创建class时对注释进行控制 -->
        <commentGenerator>
            <property name="suppressDate" value="true"/>
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>

        <jdbcConnection driverClass="${spring.datasource.driver-class-name}"
                        connectionURL="${spring.datasource.url}"
                        userId="${spring.datasource.username}"
                        password="${spring.datasource.password}">
        </jdbcConnection>

        <javaTypeResolver type="org.mybatis.generator.internal.types.MyJavaTypeResolver">
            <!-- 默认false，把JDBC DECIMAL 和 NUMERIC 类型解析为 Integer true，把JDBC DECIMAL
                和 NUMERIC 类型解析为java.math.BigDecimal -->
            <property name="forceBigDecimals" value="false"/>
        </javaTypeResolver>

        <javaModelGenerator targetPackage="com.unsys.race.database.entity"
                            targetProject="./src/main/java">
            <!-- 是否在当前路径下新加一层schem -->
            <property name="enableSubPackages" value="false"/>
            <property name="trimStrings" value="true"/>
            <!--            <property name="rootClass" value="java.io.Serializable"/>-->
            <!-- 是否生成构造函数 默认是false -->
            <property name="constructorBased" value="false"/>
        </javaModelGenerator>

        <sqlMapGenerator targetPackage="mapper"
                         targetProject="./src/main/resources">
            <property name="enableSubPackages" value="false"/>
        </sqlMapGenerator>

        <javaClientGenerator type="XMLMAPPER" targetPackage="com.unsys.race.database.mapper"
                             targetProject="./src/main/java">
            <property name="enableSubPackages" value="false"/>
        </javaClientGenerator>

        <table schema="irdb" tableName="dic_id2string" domainObjectName="DicId2String">
            <property name="useActualColumnNames" value="${extend.table.useActualColumnNames}"/>
        </table>
    </context>
</generatorConfiguration>
